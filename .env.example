PORT=3000
JWT_SECRET=your_super_secret_key_change_this_in_production
NODE_ENV=production

# Smart Search - AI Assistant (Phase 2). Optional: if missing, AI assistant
# falls back to plain keyword search instead of returning an error.
ANTHROPIC_API_KEY=your_anthropic_api_key_here
